using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ctest : MonoBehaviour
{
    //public float grade1, grade2, grade3, grade4, grade5,average;
    // Start is called before the first frame update
   
    void Start()
    {
       
        //grade1 = Random.Range(0, 100f); 
        //grade2 = Random.Range(0, 100f);
        //grade3 = Random.Range(0, 100f);
        //grade4 = Random.Range(0, 100f);
        //grade5 = Random.Range(0, 100f);
        //average = (grade5 + grade4 + grade3 + grade2 + grade1) / 5;
        //average = Mathf.Round(average * 1000f) / 1000f;//因为mathf.round是取得最近的整数，先乘以1000将小数点后移三位，后面的数字就被约掉了，再除以1000小数点再向前回移三位。
    }
    // Update is called once per frame
    void Update()
    {
       
    }
   
}
