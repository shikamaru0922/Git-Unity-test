using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AfterChangeAddHealth : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObject.FindWithTag("Player").GetComponent<CharactrStats>().templateData.currentHealth = 100;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
